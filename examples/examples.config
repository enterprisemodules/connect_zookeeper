import from zookeeper('192.168.99.100:2181') do
  all_load_balancers = "/#{client}/loadbalancers/"  # fetches an array
  firewall_ip        = "/#{client}/firewall+ip"     # Fetches a single value
end

